#!/usr/bin/env python3
"""
ValOs CLI launcher.

This wrapper should behave like the installed `valos-agent` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from valos_cli.main import main
    main()
